# XState

XState is a JavaScript library for create state machines and statecharts. 
It is a library for managing state in your application and 
provides a way to model your application as a state machine, 
which can be visualized and inspected using tools like [Stately.io](stately.io).
