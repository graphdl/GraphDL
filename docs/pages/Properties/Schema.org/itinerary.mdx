import Link from 'next/link'
  
import Grid from '@components/Grid'

# itinerary

Destination(s) ( <Link href="/Place">Place</Link> ) that make up a trip. For a trip where destination order is important use <a class="localLink" href="/ItemList">ItemList</a> to specify that order (see examples).

## Property of



