import { GlobalConfig } from 'payload/types'

export const SDK: GlobalConfig = {
  slug: 'sdk',
  admin: { group: '❪❫ Code' },
  label: '❪❫ SDK',
  fields: [
    { type: 'text', name: 'domain' },
  ],
}