import { GlobalConfig } from 'payload/types'

export const API: GlobalConfig = {
  slug: 'api',
  admin: { group: '❪❫ Code' },
  label: '❴❵ API',
  fields: [
    { type: 'text', name: 'domain' },
  ],
}